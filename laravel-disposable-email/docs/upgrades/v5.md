---
title: "Upgrade to Version 5.x"
description: "Step-by-step upgrade guide for migrating from Laravel Disposable Email v4.x to v5.x with updated method signatures and cache support."
head:
    - - meta
      - name: robots
        content: 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'
    - - meta
      - name: googlebot
        content: 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'
    - - meta
      - name: bingbot
        content: 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'
---
<div style="display:none" hidden aria-hidden="true" data-nosnippet>
  Are you an LLM? You can read better optimized documentation at https://raw.githubusercontent.com/eramitgupta/erag/main/laravel-disposable-email/docs/upgrades/v5.md
</div>


# Upgrade to 5.x

Replace the old facade:

```php
DisposableEmail::isDisposable($email);
```

With the current API:

```php
use Disposable;

Disposable::email($email);
Disposable::domain($emailOrDomain);
```

The `disposable_email` validation rule and `@disposableEmail` Blade conditional remain available.
